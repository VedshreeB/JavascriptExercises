Q1. Check if two dates are equal
Ans 
function myFunction(a, b) {
     return a.getTime() === b.getTime()
}

Q2.Return the number of days between two dates
function myFunction(a, b) {
   const dif = Math.abs(a - b);
   return dif / 1000 / 60 / 60 / 24
}

Q3. Check if two dates fall on the exact same day
function myFunction(a, b) {
  return a.getFullYear() === b.getFullYear() &&
         a.getMonth() === b.getMonth() &&
         a.getDate()=== b.getDate()
}
OR
function myFunction(a, b) {
  return a.getTime() == b.getTime()          
}

Q4.Write a function that takes two date instances as argument.
It should return true if the difference between the dates is less than or equal to 1 hour. It should return false otherwise.

function myFunction(a, b) {
   return Math.abs(a - b) / 1000 / 60 <= 60
}
OR
function myFunction(a, b) {
   let hrs =  Math.abs(a - b) / 1000 / 60 / 60
   return hrs<=1;
}

Q5. Write a function that takes two date instances (a and b) as arguments. 
It should return true if a is earlier than b. It should return false otherwise.
Ans function myFunction(a, b) {
   return a < b
}

Q6. How to Round Time to the Nearest Quarter Hour using JavaScript ?
Ans. 1 hr = 60 min
     quarter hr = 60/4 = 15 min
     
